import Theme, { colors, fonts, transition } from "./Theme";
import GlobalStyle from "./GlobalStyle";
import media from "./media";
import mixins from "./mixins";
import Section from "./Section";

export {
  Theme,
  colors,
  fonts,
  transition,
  GlobalStyle,
  media,
  mixins,
  Section,
};
